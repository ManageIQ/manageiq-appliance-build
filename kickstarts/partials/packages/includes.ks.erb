manageiq-appliance
manageiq-appliance-tools

## Review and remove packages that are required by manageiq-* rpms

epel-release

createrepo
fuse-libs

cmake                            # For rugged gem
http-parser                      # libhttp_parser.so.2 needed by nodejs
lshw                             # From epel
python3-devel                    # for building python modules in post
telnet
zip                              # build requires

# IPA RPMs for certificate support
certmonger

<% if @target != "azure" && @target != "gce" %>
cloud-init
<% end %>

<% case @target
   when "azure" %>
WALinuxAgent
<% when "ec2" %>
dracut-config-generic
<% when "hyperv" %>
hyperv-daemons
<% when "libvirt" %>
qemu-guest-agent
<% when "ovirt" %>
qemu-guest-agent
glusterfs-fuse
<% when "vsphere" %>
open-vm-tools
<% end %>
