epel-release

createrepo
freeipmi
fuse-libs
httpd
ipmitool

# TODO - remove packages needed only to compile ruby
automake
gcc
gdbm-devel
libffi-devel
libyaml-devel
ncurses-devel
openssl-devel
readline-devel
zlib-devel

ansible
ansible-runner
chrony
cifs-utils
cmake                            # For rugged gem
cockpit
cronie
http-parser                      # libhttp_parser.so.2 needed by nodejs
libcurl-devel                    # For curb gem
libssh2-devel                    # For rugged SSH support
libxml2-devel                    # For nokogiri gem
libxslt-devel                    # For nokogiri gem
logrotate
lshw                             # From epel
lvm2
memcached
mod_ssl
nmap-ncat
net-snmp
net-snmp-libs
net-snmp-utils
network-scripts
nfs-utils
nodejs                           # used for asset pipeline asset compilation - From epel
OpenIPMI
openscap-scanner
postfix                          # smtp server for sending email using localhost:25
postgresql                       # appliance section
postgresql-devel                 # for pg gem
postgresql-server                # appliance section
python3-devel                    # for building python modules in post
repmgr10
ruby
smem                             # for PSS, USS with forked processes
socat                            # for proxying remote consoles
sqlite-devel
telnet
tree
v2v-conversion-host-ansible
vim-enhanced
wget                             # build requires
yum-utils
zip                              # build requires

# IPA Identity RPMs for External Authentication
c-ares
ipa-client
sssd-dbus
mod_intercept_form_submit
mod_auth_gssapi
mod_authnz_pam
mod_lookup_identity
realmd

# IPA RPMs for certificate support
ipa-admintools
certmonger

# External Authentication - Adding Support for Active Directory
oddjob
oddjob-mkhomedir
sssd
adcli
samba-common
samba-common-tools

# External Authentication - LDAP
openldap-clients

# External Authentication - SAML
mod_auth_mellon

# External Authentication - OpenID-Connect
mod_auth_openidc

<% if @target != "azure" && @target != "gce" %>
cloud-init
<% end %>

wmi

<% case @target
   when "azure" %>
WALinuxAgent
<% when "ec2" %>
dracut-config-generic
<% when "hyperv" %>
hyperv-daemons
<% when "libvirt" %>
qemu-guest-agent
<% when "ovirt" %>
qemu-guest-agent
glusterfs-fuse
<% when "vsphere" %>
open-vm-tools
<% end %>

# For cockpit integration
cockpit-ws
cockpit-ssh

# Qpid Proton libs for Nuage provider
cyrus-sasl
cyrus-sasl-plain
qpid-proton-c-devel
