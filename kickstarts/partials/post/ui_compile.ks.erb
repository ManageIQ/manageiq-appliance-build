npm install --registry=https://registry.npm.taobao.org -g gulp bower

#ADD PROXY
cat >> ~/.bowerrc <<EOF
    {
      "proxy": "http://192.168.20.114:8123"
    }
EOF

pushd /var/www/miq/vmdb
  bower -F --allow-root install
  cp config/settings.yml.default_local_memcache config/settings.yml
  RAILS_ENV=production rake evm:compile_assets
  rake evm:compile_sti_loader
popd

# Self Service UI
pushd /opt/manageiq/manageiq-ui-self_service
  npm config set proxy=http://192.168.20.114:8123
  npm install #--registry=https://registry.npm.taobao.org
  npm config delete proxy
  bower -F --allow-root install
  gulp build
popd
